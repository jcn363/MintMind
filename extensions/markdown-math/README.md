# Markdown Math

**Notice:** This extension is bundled with MintMind. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to MintMind's built-in markdown preview and markdown cells in notebooks.
